version: '3.8'

services:
  djmanager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: djmanager
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
